
# git merge faq


## fatal: refusing to merge unrelated histories

`git merge` で

```
fatal: refusing to merge unrelated histories
```

が出た場合

```
git merge --allow-unrelated-histories target_branch
```

で 解決できる







