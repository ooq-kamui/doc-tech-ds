
# usermod


## user を group に 追加する

```
sudo usermod -aG <group-name> <user-name>
```

```
-a (--append): user に group を 追加 追加
               これがないと 現在の 他の group が外れる
-G (--groups): group name を 指定
```








